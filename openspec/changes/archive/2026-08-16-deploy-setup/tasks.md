## 1. Git Repository & Ignore Configuration

- [x] 1.1 Update `.gitignore` to exclude `pb_bin/`, `pb_data/`, `*.db*`, `build/`, `.DS_Store`, and environment secrets
- [x] 1.2 Initialize local Git repository with `git init`
- [x] 1.3 Stage project files and create initial commit

## 2. Deployment Documentation

- [x] 2.1 Write `DEPLOYMENT.md` with step-by-step instructions for deploying Vercel (Frontend SPA) and Railway (PocketBase Docker backend with persistent volume)
